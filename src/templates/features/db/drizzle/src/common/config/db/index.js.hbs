{{#if ts}}
import { env } from "@/common/config/env";
import { UserTable } from "@/features/user/user.schema";
{{else}}
import { env } from "../env.js";
import { UserTable } from "../../../features/user/user.schema.js";
{{/if}}
import { drizzle } from 'drizzle-orm/node-postgres';

const db = drizzle(env.DATABASE_URL{{#if auth.enable}}, {
  schema: {
    users: UserTable,
  },
}{{/if}});

export default db;