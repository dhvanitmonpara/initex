# {{name}} is scaffolded with [Initex](https://www.npmjs.com/package/initex).

## How to run?

1. Install dependencies:

   ```bash
   {{packageManager}} install
   ```
2. Setup the database:

   ```bash
   {{packageManager}} run db:up
   ```

3. Run the project:

   ```bash
   {{packageManager}} run dev
   ```

4. Open http://localhost:{{port}} in your browser to view the project.