import { User } from "@/shared/types/User";

declare global {
  namespace Express {
    interface Request {
      {{#if auth.enable}}user?: Omit<User, "password">;
      {{/if}}id: string;
    }
  }
}